;does not work as of 3.16.0 or so, very easy to fix however

SendPacket PROC
; RCX = NetClass //Only real param in func, we use 3 to make replacing ptr easier
; RDX =  Buff
; R8 = Len
	
	!removed with respect for the game developers!

SendPacket ENDP

